import './CSS/InfoCard.css'

const InfoCard = ({title}) => {
    return (        
        <div className='infoCard'>
            <h2>{title}</h2>
            
        </div>
    )
}

export default InfoCard